{
  "name": "plan-na-milosc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "rm -rf .next && next build",
    "start": "NODE_ENV=production node app.js",
    "export": "rm -rf .next && next build && next export",
    "clean": "rm -rf .next",
    "restart": "devil www restart plannamilosc.pl"
  },
  "dependencies": {
    "@emotion/react": "^11.1.5",
    "@emotion/styled": "^11.1.5",
    "emotion-normalize": "^11.0.1",
    "facepaint": "^1.2.1",
    "next": "10.0.9",
    "next-share": "^0.0.2",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-facebook-pixel": "^1.0.4"
  },
  "devDependencies": {
    "@emotion/babel-plugin": "^11.2.0",
    "express": "^4.17.1"
  }
}
